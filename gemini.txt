根据提供的功能实现代码，修改pipeline.ipynb为ui显示交互版本，要求使用pyqt5架构1.开始界面需要一个单独的窗口设计，提供选择文件夹，并根据文件夹中的视频文件自动生成正则表达式，以及选择两种流程（我给的minian和另一个还未设计的流程），然后选择开始后再打开主流程显示界面2.对于主流程显示界面，要求按照运行分成步骤如下：loading videos and visualization、loading videos and visualization、denoise、background removal、Motion Correction、generating over-complete set of seeds、peak-noise-ratio refine、ks refine、merge seeds、visualization of initialization、first spatial update、first temporal update、second spatial update、second temporal update、最后数据保存，这是我对于可视化步骤的要求，下具体阐释可视化的要求，对于每一个步骤要求使用同一个界面布局，如上图，左侧为参数调试、步骤选择、按键分布，右侧为可视化窗格，对于参数设置，要求对于当前步骤显示所有涉及参数，首先导入默认参数设置（这个默认参数在窗口的左上角应该提供选项包含选择默认配置文件，对于每个文件夹数据处理我们需要进行一个建库操作，主要存有处理结果和参数设置的配置文件，方便统一文件夹数据再次处理的参数导入和其他共享参数工程的参数导入），在每个步骤运行前可手动输入调整任意参数，如果是文件读取相关参数，如果错误需要避免死机，而是弹出报错弹窗，让用户修改路径再次进行运行，每个步骤的按键主要有下一步、运行当前步骤、上一步、运行所有步骤，上下步骤的功能即为切换步骤，运行当前步骤首先对当前参数设置检查是否相对当前库的参数设置有更新，如果则写入，然后调用运行当前步骤，注意由于步骤大概率需要反复调试，对于每个步骤都要保存运行前数据，然后允许进行运行当前步骤后修改参数再次运行，注意需要保证程序安全，只能通过下一步按键达到未运行步骤，然后提供一个选择框，可以下拉选择任何已运行步骤查看效果，并且跳转后可以再次运行，但是运行后其后的步骤需要标注为未运行，可以清除对应缓存。当按下运行全部步骤时，在后台计算所有步骤，参数直接使用库设置的参数，因此当一个新建的仓库打开后应该提供一个弹窗选择默认参数（当前提供的pipeline的参数）或者选择复制其他仓库的配置文件，然后才能运行。点击运行全部后完成应该跳出弹窗提示，然后可以通过下拉步骤选择直接查看所有步骤效果，以及修改和重新运行。3.对于所有可视化的要求如下：要求显示运行结果的视频，通过开始、暂停、快进、快退控制，同时需要可以直接拖动进度条跳转。对于loading videos and visualization、loading videos and visualization、denoise、background remova显示运行后结果视频，对于Motion Correction显示运行前后对比视频，对比方法就是拆分左右两个视频进行显示，对于generating over-complete set of seeds、ks refine、merge seeds、visualization of initialization即显示原流程中的种子点显示，对于peak-noise-ratio refine显示当前噪声频率下分离的信号和噪声曲线（原流程里有），对于first spatial update、first temporal update、second spatial update、second temporal update要拆分成两步，第一步可视化显示当前稀释度惩罚下的效果，即原流程下的parameter exploration部分，然后在使用参数进行更新，可视化显示原流程中四宫格的对比图，注意我这里的意思是将这四个大步骤都需要拆分开4.对于保存最终数据，默认采用原来的保存方式，然后提供一个保存为csv文件的选项，minian_ds = open_minian(dpath)minian_ds["C"].rename("C").to_series().reset_index().to_csv("C.csv")，这是一个保存C矩阵的示例代码4.最后代码可读性方面，ui部分的代码可以通过文件分工实现，对于我提供的这些minian库代码也可以作适应性修改，暂时所有交互都采用中文，可以在各个增加终端的输出交互运行情况和错误原因，可以尽可能细节得通过print向开发者交互。最后，我提供的代码visualization你是肯定要修改的，除了他和pipeline，其他文件我都放在主程序同文件夹的minian_core的文件夹中，调用时注意。